# 正则表达式速查笔记

## 📝 基础语法速记

### 字符匹配
| 符号 | 含义                    | 示例            |
| ---- | ----------------------- | --------------- |
| `.`  | 任意字符（除换行符）    | `a.c` → "abc"   |
| `\d` | 数字 `[0-9]`            | `\d\d` → "12"   |
| `\D` | 非数字                  | `\D+` → "abc"   |
| `\w` | 单词字符 `[a-zA-Z0-9_]` | `\w+` → "hello" |
| `\W` | 非单词字符              | `\W` → "!"      |
| `\s` | 空白字符                | `\s+` → "  "    |
| `\S` | 非空白字符              | `\S+` → "abc"   |

### 字符集合
```regex
[aeiou]        # 匹配任意元音
[0-9]          # 匹配数字
[a-z]          # 匹配小写字母
[^abc]         # 匹配除abc外的字符
```

## 🔢 量词与重复

| 量词    | 含义      | 贪婪型   | 懒惰型    |
| ------- | --------- | -------- | --------- |
| `*`     | 0次或多次 | `.*`     | `.*?`     |
| `+`     | 1次或多次 | `.+`     | `.+?`     |
| `?`     | 0次或1次  | `a?`     | `a??`     |
| `{3}`   | 恰好3次   | `a{3}`   | -         |
| `{2,}`  | 至少2次   | `a{2,}`  | `a{2,}?`  |
| `{2,4}` | 2到4次    | `a{2,4}` | `a{2,4}?` |

**💡 记忆技巧：**
- `*` = 任意次数
- `+` = 至少一次  
- `?` = 可有可无
- `{n}` = 精确控制

## 📍 位置锚点

| 锚点 | 含义       | 示例       |
| ---- | ---------- | ---------- |
| `^`  | 字符串开始 | `^start`   |
| `$`  | 字符串结束 | `end$`     |
| `\b` | 单词边界   | `\bword\b` |
| `\B` | 非单词边界 | `\Bword\B` |

## 🎯 分组与引用

### 分组类型
```regex
(abc)           # 捕获分组
(?:abc)         # 非捕获分组
(?<name>abc)    # 命名分组
```

### 反向引用
```regex
(\w+) \1        # 匹配重复单词 "hello hello"
(['"]).*?\1     # 匹配引号内容 "'text'" 或 '"text"'
```

## 🚩 修饰符

| 标志 | 含义       | 示例             |
| ---- | ---------- | ---------------- |
| `i`  | 忽略大小写 | `/hello/i`       |
| `g`  | 全局匹配   | 匹配所有出现     |
| `m`  | 多行模式   | `^`/$ 匹配行首尾 |
| `s`  | 单行模式   | `.` 匹配换行符   |

## 🔍 断言（Lookaround）

| 断言类型 | 语法       | 含义         |
| -------- | ---------- | ------------ |
| 正向前瞻 | `(?=...)`  | 后面必须跟着 |
| 负向前瞻 | `(?!...)`  | 后面不能跟着 |
| 正向后顾 | `(?<=...)` | 前面必须跟着 |
| 负向后顾 | `(?<!...)` | 前面不能跟着 |

**📌 实例：**

```regex
\d+(?=px)       # 匹配 "100" 在 "100px" 中
\d+(?!px)       # 匹配 "100" 在 "100em" 中
(?<=\$)\d+      # 匹配 "100" 在 "$100" 中
(?<!\$)\d+      # 匹配 "100" 在 "100" 中
```

## 💡 常用模式模板

### 邮箱验证
```regex
^\w+([.-]?\w+)*@\w+([.-]?\w+)*(\.\w{2,3})+$
```

### 手机号码（中国）
```regex
^1[3-9]\d{9}$
```

### URL 匹配
```regex
https?://[^\s]+
```

### 日期（YYYY-MM-DD）
```regex
^\d{4}-\d{2}-\d{2}$
```

### 密码强度（8位+大小写+数字+特殊字符）
```regex
^(?=.*[a-z])(?=.*[A-Z])(?=.*\d)(?=.*[@$!%*?&])[A-Za-z\d@$!%*?&]{8,}$
```

### 中文字符
```regex
[\u4e00-\u9fa5]
```

## ⚡ 实用技巧

### 1. 贪婪 vs 懒惰
```regex
<.*>     # 贪婪：匹配整个 "<div>content</div>"
<.*?>    # 懒惰：分别匹配 "<div>" 和 "</div>"
```

### 2. 验证数字范围
```regex
^([1-9]|[1-9]\d|100)$    # 1-100
```

### 3. 提取关键信息
```regex
姓名：(\w+)\s*电话：(\d+)   # 提取姓名和电话
```

## 🛠️ 代码示例

### JavaScript
```javascript
// 创建正则
const regex = /pattern/flags;
const regex2 = new RegExp('pattern', 'flags');

// 常用方法
const str = 'test string';
regex.test(str);        // 返回布尔值
str.match(regex);       // 返回匹配结果
str.replace(regex, 'new'); // 替换
```

### Python
```python
import re

pattern = re.compile(r'pattern')
result = pattern.search('text')  # 搜索
matches = pattern.findall('text') # 所有匹配
new_text = pattern.sub('new', 'text') # 替换
```

## 🎨 可视化记忆

### 基础结构
```
/   pattern   /   flags
    └── 匹配模式    └── 修饰符
```

### 量词层次
```
? (0或1) < {n} (精确) < * (0或多) < + (1或多)
```

### 边界概念
```
^ 开头 ← 文本 → 结尾 $
\b 单词边界 ← 单词 → 单词边界 \b
```

## ⚠️ 常见陷阱

1. **忘记转义特殊字符**：`\.` 不是 `.`
2. **贪婪匹配问题**：用 `.*?` 代替 `.*`
3. **锚点使用错误**：`^pattern$` 用于完全匹配
4. **字符集混淆**：`[a-z]` 匹配小写字母，不是单词

## 🔄 快速测试步骤

1. **明确需求**：要匹配什么？要提取什么？
2. **选择工具**：使用在线测试器（如 regex101.com）
3. **逐步构建**：从简单模式开始，逐步添加复杂度
4. **测试边界**：测试空值、极端值、特殊字符
5. **优化性能**：避免过度复杂的表达式

---

**📌 记住：正则表达式就像一门语言，多练习才能熟练！**

这个笔记设计为便于快速查阅，包含了正则表达式最常用的功能和语法。建议打印或保存为参考卡片。